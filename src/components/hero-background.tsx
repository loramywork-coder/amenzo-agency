"use client";

// Transparent hero — lets the global WebGL smoke show through completely
// No cursor light, no orbs, no vignette — just transparent
export default function HeroBackground() {
  return null;
}
